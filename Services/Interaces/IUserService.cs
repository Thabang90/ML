using QuestionExplorer.Entities;

namespace QuestionExplorer.Services.Interaces
{
    public interface IUserService
    {
        //Task<User> LoginAsync(User user);
        //Task<User> UpsertUsers(User user);
    }
}
